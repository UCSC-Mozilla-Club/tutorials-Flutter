import 'package:flutter/material.dart';

class ChatroomScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("No Chatroom right now"),
      ),
    );
  }
}
