import 'package:flutter/material.dart';

class RepositoriesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("In next lesson we can grab repositories from github api"),
      ),
    );
  }
}
